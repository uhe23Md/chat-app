# 第 6 阶段完成 · 实时聊天跑起来了

> 这个阶段实际上把任务书的**第 6、7、8 三个阶段一次做完了**：
> 实时收发、历史消息、在线用户、多房间、私聊 —— 全部打通。
>
> 验收结果：**67 项浏览器端到端测试全过，0 失败。**

---

## 一、先看最重要的：它真的能两个窗口互通

无头浏览器开了两个**完全独立的浏览器实例**，各自登录不同账号，实测截图：

**窗口 A（alice 视角）** —— 自己发的消息在右边蓝色，对方在左边白色

```
   # 学习区                                    ● 已连接   [A] alice
   ─────────────────────────────────────────────────────────────
   房间              18:14  alice  [E]
   # 学习区                  ┌─────────────────────┐
   # 游戏区                  │ A 发的第一条消息 ...│  ← 蓝色靠右
   # 闲聊区                  └─────────────────────┘
                    [E] alice 18:14
                    ┌─────────────────────┐
                    │ B 回复的消息 ...    │        ← 白色靠左
                    └─────────────────────┘
```

**窗口 B（bob 视角）** —— 同一条消息，左右完全对调

```
   房间              18:14  alice  [E]
   # 学习区                  ┌─────────────────────┐
   # 游戏区                  │ A 发的第一条消息 ...│  ← 白色靠左
   # 闲聊区                  └─────────────────────┘
                    [E] bob  18:14
                    ┌─────────────────────┐
                    │ B 回复的消息 ...    │        ← 蓝色靠右
                    └─────────────────────┘
```

**这就是实时通信成功最直观的证明**：同一条消息，在自己那边是"我的"，在对方那边是"他的"。

---

## 二、这一步新增了什么

### 后端

| 文件 | 干什么的 |
|---|---|
| `server/queries.js` | **数据访问层**。所有 SQL 集中在这一个文件里 |
| `server/routes/rooms.routes.js` | `GET /api/rooms` — 房间列表 |
| `server/routes/messages.routes.js` | `GET /api/messages`、`GET /api/private-messages` |
| `server/sockets/index.js` | **实时通信核心**：连接鉴权 + 三个事件 |

### 前端

| 文件 | 干什么的 |
|---|---|
| `public/chat.html` | 聊天主界面（三栏布局） |
| `public/js/chat.js` | 聊天逻辑（约 700 行，注释很全） |

### 测试

| 文件 | 覆盖 |
|---|---|
| `test-socket.js` | Socket 层 53 项（多客户端模拟） |
| `.verify/verify-stage6.cjs` | 浏览器端到端 67 项（真开浏览器） |
| `test-cleanup.js` | 清理测试账号（`npm run clean:test`） |

---

## 三、几个关键设计，理解了这个项目就懂了

### 1. 为什么历史消息走 REST，新消息走 Socket？

这两种需求的**形态完全不同**：

```
拉历史消息    一次性请求-响应，拉完就结束   →  适合 HTTP
              （有状态码、能缓存、curl 就能调试）

收新消息      服务端主动推，不知道何时来     →  才需要 WebSocket
              （长连接、双向）
```

把历史消息硬塞进 Socket 事件里，等于把"查询"包装成"订阅"，
出错只能靠回调拿错误对象，没有 400/500 这种明确状态码，调试会很难受。

**所以前端有两个不同的"消息通道"，这是刻意设计的，不是重复。**

### 2. 在线用户为什么要用两张 Map？

```js
onlineUsers: Map<userId, Set<socketId>>   // 一个人可能有多个连接
socketMeta:  Map<socketId, { userId, username, roomId, avatar }>
```

**为什么值是 `Set` 而不是单个 id？**

因为一个人可以同时开很多窗口（电脑 + 手机 + 多个标签页）。
如果只存一个 id，后打开的会覆盖先打开的 ——
**关掉其中一个窗口，服务端就会以为"这个人下线了"，把还在线的窗口也从列表里抹掉。**

真正的离线判定是：**这个人的 Set 变成空的**。

这个场景我专门写了测试：
- bob 开两个连接 → 在线列表仍显示 2 人（按用户去重）
- 关掉一个 → bob 还在列表里
- 全部关掉 → 才从列表消失

三个断言全过。

### 3. 怎么防止"往自己不在的房间发消息"？

这是个容易忽略的越权漏洞。如果直接信客户端传的 `roomId`：

```
攻击者：人在「闲聊区」，但构造 { roomId: 1, content: "..." }
       → 消息就发进「学习区」了，房间隔离形同虚设
```

**修复**：服务端以自己记录的 `meta.roomId` 为准，不匹配就拒绝。

```js
if (meta.roomId !== roomId) {
  return reply({ ok: false, error: '你不在这个房间里，请先切换房间' });
}
```

测试里专门验证了这条：`不能往自己不在的房间发消息（防越权）` ✅

### 4. 私聊为什么只推给两个人？

```js
// 只推给接收者的所有连接
emitToUser(io, toUserId, 'new_private_message', message);

// 发送者的其他窗口也推一份，但排除发起这次请求的那个 socket
for (const sid of senderSockets) {
  if (sid === socket.id) continue;   // ← 这个排除很关键
  io.to(sid).emit('new_private_message', message);
}
```

**为什么要排除当前 socket？**
因为当前窗口的渲染是由 ack 回调负责的。如果不排除，
同一条私聊会被渲染两次（一次来自 ack，一次来自广播）。

前端还有一层保险：渲染前检查 `data-msg-id` 是否已存在，重复的直接跳过。

**安全上**：私聊绝不广播给房间，测试验证了"carol 收到 0 条"。

### 5. 发送者自己也会收到自己的消息广播 —— 这不是 bug

```js
// 广播给房间所有人，包括发送者自己
io.to(roomChannel(roomId)).emit('new_message', message);
```

**为什么包括自己？** 因为发送者可能开了 3 个窗口，
统一由服务端推送来渲染，3 个窗口才能保持一致。

**所以前端千万别自己往列表里插消息** —— 那会导致当前窗口显示两条。

---

## 四、怎么自己跑起来看

### 1. 启动服务

```bash
cd C:\Users\Administrator\Desktop\1\chat-app
npm start
```

看到这样的输出就对了：

```
[db] 数据库就绪: ...\chat-app\chat.db
[db] 当前房间数: 3
[socket] Socket.IO 实时层已就绪

========================================
  聊天室后端已启动
  http://localhost:3000
========================================
```

### 2. 体验"两个窗口实时通信"

1. 浏览器打开 `http://localhost:3000`
2. 注册账号 `小明`，进聊天大厅
3. **再开一个无痕窗口**（`Ctrl + Shift + N`），打开同一个地址
4. 注册账号 `小红`
5. 现在两个窗口都在线了 —— 看右边的在线用户列表，应该有 2 个人
6. 在小红那个窗口发一句话 → **小明的窗口应该立刻看到**

试试这些：

- 点在线用户头像 → 打开私聊
- 点左边房间名 → 切换房间，在线列表会实时更新
- 刷新页面 → 历史消息还在
- 在小明窗口往上翻着看历史，小红发消息 → **不会把你强行拉到底部**（这个体验细节做了）

### 3. 跑测试

```bash
npm test                          # 后端全部（71 项）

cd .verify
node verify-stage5.cjs            # 登录注册页（52 项，约 1.5 分钟）
node verify-stage6.cjs            # 聊天功能（67 项，约 40 秒）
```

---

## 五、实测通过的关键断言（挑几条重要的）

```
【★ 核心：A 发消息，B 立刻收到】
  PASS  ★ B 窗口立刻收到 A 的消息
  PASS  B 看到的消息发送者正确
  PASS  这条消息在 B 那边显示为「别人的」（靠左）
  PASS  A 那边这条消息显示为「自己的」（靠右）
  PASS  两边拿到的是同一条消息（id 一致）
  PASS  ★ A 窗口立刻收到 B 的回复
  PASS  两个窗口的消息内容与顺序完全一致

【刷新后历史消息还在】
  PASS  ★ 刷新后能看到之前发的消息（从数据库读回）
  PASS  刷新重连后在线列表恢复正常

【在线用户实时更新与去重】
  PASS  bob 多开一个连接时，在线列表仍显示 2 人（按用户去重）
  PASS  关掉额外连接后 bob 仍在列表（浏览器窗口还开着）

【私聊】
  PASS  B 收到私聊提示（弹窗未开时提示用户）
  PASS  ★ B 打开私聊后能看到 A 发来的私聊
  PASS  ★ A 实时收到 B 的私聊回复

【切换房间】
  PASS  切换后看不到旧房间的消息（房间隔离）
  PASS  ★ A 切走后 B 的在线列表实时变成只剩自己

【XSS 防护】
  PASS  消息里没有生成真实的 <img> 元素
  PASS  消息里没有生成真实的 <script> 元素
  PASS  onerror 没有被执行
  PASS  内联 script 没有被执行

【移动端布局（390×844）】
  PASS  汉堡菜单按钮显示了（移动端专属）
  PASS  房间列表变成了抽屉（position: fixed）
  PASS  输入框字号 >= 16px（防止 iOS 自动缩放）
  PASS  没有横向溢出
```

---

## 六、踩的坑（都是我自己写测试时踩的，记下来免得重犯）

### 1. `withEdge()` 返回时就把浏览器杀了

第一次写端到端测试，我用：

```js
const [winA, winB] = await Promise.all([
  withEdge(async (cdp) => { ...测试逻辑... }),
  withEdge(async (cdp) => { ...测试逻辑... }),
]);
// 拿到 winA.cdp / winB.cdp，打算继续在这两个窗口之间操作
```

结果：前 17 项断言全过，然后 `CDP timeout: Runtime.evaluate`。

**根因**：`withEdge` 的 `finally` 块里有 `edge.kill()`。
`Promise.all` resolve 的那一刻，两个浏览器**即将被销毁** ——
它返回的 `cdp` 已经是个死连接了。

**修复**：给 `_cdp.cjs` 加了 `launchBrowser()`，返回 `{ cdp, close, port }`，
由调用方用 `try/finally` 自己管生命周期。凡是需要多浏览器长期共存的场景都用它。

**还有个坑中坑**：并行多实例**必须传不同的调试端口**，
否则第二个实例会连到第一个的调试端口 —— 两个"窗口"其实是同一个进程。

### 2. 测试里"等事件超时"，先怀疑自己的测试逻辑

第 10 节的 XSS 测试一直等不到消息。查了半天服务端，最后发现是：
第 8 节让 alice 切到了「游戏区」，而 bob 还在「学习区」，
我却让 alice 发到游戏区、然后等 bob 收到 —— **两人根本不在一个房间**。

教训：写多客户端测试时，每个"等消息"的断言前都要确认收发双方在同一房间。

### 3. 测试清理"成功执行但清了 0 个"

`.verify/verify-stage6.cjs` 用账号前缀 `e2eA`/`e2eB`，
但 `test-cleanup.js` 的前缀列表里没有这两个，所以清理静默失败，
测试账号悄悄堆在数据库里。已补上。

---

## 七、下一步可以做什么

现在是第 9 阶段（测试与优化）和第 10 阶段（部署）。可选：

**优化类**
- 消息已读回执
- "正在输入…" 提示（`typing` 事件，设计文档里已经预留了）
- 未读消息红点（现在只在输入框下方给文字提示）
- 历史消息"加载更多"（后端已经支持 `before` 游标分页，前端还没接）
- 消息时间分组（连续同一人发的消息合并显示）

**部署类**
- 推到 GitHub
- 部署到 Render / Railway
- ⚠️ 注意：`node:sqlite` 需要 Node ≥ 22.5；而且免费层容器重启会丢数据，
  正式用要考虑挂持久卷或换成 Postgres

想做哪个跟我说。
