# 第 5 阶段 · 前端界面改造指南

> 这份文档是给你自己动手改界面用的。
> 后端接口、请求封装（`api.js`）、页面逻辑（`auth.js`）都已经写完并能跑通，
> 你只需要在 HTML 和 CSS 上折腾。

---

## 一、先跑起来，看看现状

```bash
cd C:\Users\Administrator\Desktop\1\chat-app
npm start
```

浏览器打开 `http://localhost:3000` → 会自动跳到登录页。

看几个页面：

| 地址 | 看什么 |
|---|---|
| `http://localhost:3000/login.html` | 登录页 |
| `http://localhost:3000/register.html` | 注册页 |
| `http://localhost:3000/index.html` | 入口跳转页 |

**现在就能用的功能：** 注册、登录、登录态保持、错误提示、表单回车提交。

---

## 二、文件职责，别改错地方

```
public/
├── login.html        登录页结构
├── register.html     注册页结构
├── index.html        入口跳转
├── img/
│   └── logo.png      ★ 品牌图标（你给的那个 A），当 logo + favicon 用
├── css/
│   └── style.css     ★ 所有样式都在这里，你主要改这个
└── js/
    ├── api.js        ★ 别动 —— 请求封装，改坏了所有页面都崩
    ├── auth.js          登录注册页的逻辑，想加交互可以改
    └── chat.js          第 6 阶段才创建
```

**改动优先级：**

1. **`css/style.css`** —— 想换配色、圆角、字号，改最上面的 `:root` 变量就行，不用翻到下面
2. **`login.html` / `register.html`** —— 想加"记住我"、忘记密码、第三方登录按钮，加在这里
3. **`js/auth.js`** —— 想加前端校验规则、改提示文案，改这里
4. **`js/api.js`** —— 一般不用动

---

## 三、品牌图标（已接入）

你给的那张「黑底白 A」已经装好了，现在同时用在两个地方：

| 用处 | 位置 | 效果 |
|---|---|---|
| 卡片顶部的 Logo | `login.html` / `register.html` 里的 `<img class="auth-logo">` | 登录/注册卡片正上方，52×52 |
| 浏览器标签页图标（favicon） | 三个页面 `<head>` 里的 `<link rel="icon">` | 标签页左边那个小图标 |
| 手机"添加到主屏幕"图标 | `<link rel="apple-touch-icon">` | iOS/安卓存桌面时的图标 |

源文件是 `public/img/logo.png`（192×192）。**想换图标只需要覆盖这一个文件**，
三个页面自动跟着变，不用改任何 HTML。

Logo 的尺寸和圆角在 `style.css` 的 `.auth-logo` 里：

```css
.auth-logo {
  width: 52px;          /* 想大一点就改这里，比如 64px */
  height: 52px;
  border-radius: 16px;  /* 圆角，设成 50% 会变正圆 */
  box-shadow: 0 6px 18px rgba(31, 36, 48, 0.14);  /* 底下那层淡阴影 */
}
```

> 注：`.auth-logo` 以前是个渐变色方块里面放 emoji 💬，现在已经改成纯图片了。
> 如果你看到"半透明描边"效果是因为图片自带白底，`outline` 那行是为了让它在浅色背景上不糊掉。

---

## 四、配色：改一个变量，全站生效

打开 `style.css` 最上面这一段：

```css
:root {
  /* 主色：一个偏冷的蓝，聊天软件常用 */
  --brand: #4f6ef7;
  --brand-dark: #4058d4;
  --brand-light: #eef1fe;

  /* 中性色 */
  --bg: #f4f5f9;
  --surface: #ffffff;
  --surface-2: #fafbfd;
  --border: #e5e7ef;
  --text: #1f2333;
  --text-2: #6b7280;
  --text-3: #9ca3af;

  /* 状态色 */
  --success: #16a34a;
  --danger: #dc2626;
  --online: #22c55e;

  /* 尺寸 */
  --radius: 12px;
  --radius-sm: 8px;
  --radius-lg: 18px;

  /* 布局宽度 */
  --sidebar-w: 200px;   /* 左侧房间列表宽度 */
  --online-w: 190px;    /* 右侧在线用户宽度 */
  --header-h: 56px;     /* 顶栏高度 */
}
```

### 三个可以直接抄的配色方案

**方案 A · 微信绿**（把上面的 brand 三行换掉）

```css
--brand: #07c160;
--brand-dark: #06ad56;
--brand-light: #e8f8ef;
```

**方案 B · 紫罗兰**（Discord 那挂）

```css
--brand: #7c5cff;
--brand-dark: #6a4ae0;
--brand-light: #f0ecff;
```

**方案 C · 暖橙**（Telegram 那挂）

```css
--brand: #f97316;
--brand-dark: #ea6a0c;
--brand-light: #fff3e8;
```

**顺手把圆角也调一下：** 想要更"卡片感"就把 `--radius` 调到 `16px`，想要更"工具感"就调到 `8px`。

**深色模式**（在 `style.css` 末尾加这段，浏览器切到深色主题时自动生效）：

```css
@media (prefers-color-scheme: dark) {
  :root {
    --bg: #16181d;
    --surface: #1e2128;
    --surface-2: #252932;
    --border: #333843;
    --text: #e8eaed;
    --text-2: #9aa0aa;
    --text-3: #6b7280;
    --brand-light: #2a3358;
  }
  .auth-page {
    background:
      radial-gradient(circle at 20% 20%, #1e2a45 0%, transparent 45%),
      radial-gradient(circle at 80% 80%, #1a2e35 0%, transparent 45%),
      var(--bg);
  }
  .auth-logo { box-shadow: 0 6px 18px rgba(79, 110, 247, 0.2); }
}
```

---

## 四、页面结构说明（想改结构时看这里）

### 登录页结构

```html
<body class="auth-page">          ← 全屏居中容器，带光晕背景
  <div class="auth-card">          ← 白色卡片
    <div class="auth-logo">💬</div>  ← 渐变圆角方块 + emoji
    <h1 class="auth-title">欢迎回来</h1>
    <p class="auth-subtitle">登录后进入聊天大厅</p>

    <div class="alert alert-error" id="alertBox"></div>
    <!-- ↑ 错误提示条。默认 display:none，加 .show 类才显示。
         auth.js 里用 showError() 控制，不要手写 style -->

    <form id="loginForm" novalidate>
      <div class="field">
        <label for="username">用户名</label>
        <input class="input" type="text" id="username" ...>
      </div>
      <div class="field">
        <label for="password">密码</label>
        <input class="input" type="password" id="password" ...>
      </div>
      <button class="btn btn-primary btn-block" type="submit" id="submitBtn">
        登录
      </button>
    </form>

    <p class="auth-footer">
      还没有账号？<a href="/register.html">立即注册</a>
    </p>
  </div>

  <script src="/js/api.js"></script>
  <script src="/js/auth.js"></script>
  <script>AuthPage.initLoginForm();</script>
</body>
```

### ⚠️ 改结构时必须保留的 id

`auth.js` 靠这些 id 找元素，**改了名字功能就废了**：

| id | 用途 |
|---|---|
| `loginForm` / `registerForm` | 表单容器，绑定 submit 事件 |
| `username` / `password` / `password2` | 输入框，读值用 |
| `submitBtn` | 提交按钮，做加载态和禁用 |
| `alertBox` | 提示条，显示错误/成功信息 |

**想加新元素随便加，但这六个改名要同步改 `auth.js`。**

### 想加"记住我"？改三处

**1. `login.html` 里加复选框**（放在密码框和按钮之间）：

```html
<div class="field-row">
  <label class="checkbox">
    <input type="checkbox" id="rememberMe">
    <span>记住我</span>
  </label>
  <a href="#" class="forgot-link">忘记密码？</a>
</div>
```

**2. `style.css` 里加样式**：

```css
.field-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin: -6px 0 18px;
  font-size: 13px;
}

.checkbox {
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--text-2);
  cursor: pointer;
}

.checkbox input {
  width: 15px;
  height: 15px;
  accent-color: var(--brand);  /* 让勾选框跟着主色走 */
  cursor: pointer;
}

.forgot-link {
  color: var(--text-2);
  font-size: 13px;
}
.forgot-link:hover {
  color: var(--brand);
}
```

**3. `api.js` 里的 `setToken` 加个开关**（找到 `setToken` 函数）：

```js
function setToken(token, remember) {
  // 记住我 → localStorage（关浏览器还在）
  // 不记住 → sessionStorage（关标签页就没了）
  const store = remember === false ? sessionStorage : localStorage;
  store.setItem(TOKEN_KEY, token);
}
```

同时 `getToken()` 也要改成两个都查：

```js
function getToken() {
  return localStorage.getItem(TOKEN_KEY) || sessionStorage.getItem(TOKEN_KEY);
}
```

`login.html` 里传参：

```js
const remember = document.getElementById('rememberMe').checked;
await API.login(username, password, remember);
```

> ⚠️ 这一改动会连带影响第 6 阶段的 Socket 连接逻辑（它也要读 token）。
> 所以要么现在就改，要么等第 6 阶段做完再改 —— **别做一半停手。**

---

## 六、CSS 类名速查表

改样式时对着这张表找，不用翻源码：

### 通用

| 类名 | 效果 |
|---|---|
| `.btn` | 按钮基础样式（高度 42px、圆角、过渡） |
| `.btn-primary` | 主色按钮 |
| `.btn-block` | 撑满整行 |
| `.input` | 输入框（含聚焦时的蓝色光晕） |
| `.field` | 输入框外层容器（含 label 的间距） |
| `.alert` / `.alert-error` / `.alert-success` | 提示条，需加 `.show` 才显示 |
| `.spinner` | 加载转圈动画 |

### 登录/注册页

| 类名 | 效果 |
|---|---|
| `.auth-page` | 全屏居中 + 光晕背景 |
| `.auth-card` | 白色卡片（最大宽 380px） |
| `.auth-logo` | 渐变圆角方块 |
| `.auth-title` / `.auth-subtitle` | 标题 / 副标题 |
| `.auth-footer` | 底部"还没有账号？"那行 |

### 聊天页（第 6 阶段用，现在可以预览）

| 类名 | 效果 |
|---|---|
| `.chat-page` | 整页 flex 纵向布局 |
| `.chat-header` | 顶栏 |
| `.chat-body` | 三栏容器 |
| `.sidebar` / `.online-panel` | 左栏 / 右栏 |
| `.main` | 中间消息区 |
| `.msg` / `.msg.self` | 消息气泡（自己的加 `.self` 靠右） |
| `.msg-system` | 系统消息（居中灰字） |
| `.composer` | 底部输入区 |
| `.modal-mask` / `.modal` | 私聊弹窗 |

---

## 六、几个容易改坏的地方

### 1. 别用 `innerHTML` 渲染用户消息

第 6 阶段渲染消息时，**一定用 `textContent`**：

```js
// ❌ 危险：消息里如果有人发 <img src=x onerror=alert(1)>，脚本就执行了
bubble.innerHTML = msg.content;

// ✅ 安全：当成纯文本
bubble.textContent = msg.content;
```

这叫 XSS（跨站脚本攻击）。聊天室是用户输入最密集的地方，这条必须守住。

### 2. 移动端的输入框字号别小于 16px

`style.css` 里有这段：

```css
@media (max-width: 768px) {
  .composer textarea,
  .input {
    font-size: 16px;
  }
}
```

**别删。** iOS Safari 在输入框字号 < 16px 时会自动放大整个页面，用户点一下输入框屏幕就"跳"一下，很烦。

### 3. `style.css` 里已经写好了移动端三栏变单栏

```css
@media (max-width: 768px) {
  .sidebar,
  .online-panel {
    position: fixed;
    transform: translateX(-100%);  /* 默认藏起来 */
  }
  .sidebar.open { transform: translateX(0); }  /* 加 .open 才滑出来 */
}
```

所以第 6 阶段写 `chat.js` 时，点菜单按钮要给侧栏加 `.open` 类，同时给遮罩 `.drawer-mask` 加 `.show`。

---

## 七、你自己动手的检查清单

改完之后挨个过一遍：

- [ ] 登录页能正常显示，样式没崩
- [ ] 注册页能正常显示
- [ ] 注册一个新用户，成功后跳到登录页
- [ ] 用新用户登录，跳转成功
- [ ] 输错密码，页面显示红色提示，按钮恢复可点
- [ ] 浏览器窗口拉到手机宽度（或用 F12 的设备模拟），布局不错乱
- [ ] 刷新页面，登录态还在（第 6 阶段做完才能完整验证）
- [ ] 改配色后，按钮、链接、聚焦光晕、消息气泡都跟着变色了

**每改一处就刷新看一眼。** 一次性改完再刷新，出问题会不知道是哪个改动导致的。

---

## 九、需要我帮忙的时候

以下情况直接说：

- **想让某个页面变成某种具体风格** —— 给我一个参考网站或截图，我照着重做
- **想加具体功能** —— 比如"记住我"、"头像上传"、"密码强度条"，说一声我来写
- **样式在某个尺寸下崩了** —— 告诉我窗口尺寸和现象，我来查
- **想看第 6 阶段的聊天页长什么样** —— `style.css` 里聊天页的样式已经全写好了，可以先进第 6 阶段看效果，回头再调样式

---

## 附：现在就能验证的东西

后端接口是 100% 可用的（18 项自动化测试全过）。你想验证前端有没有连对，打开浏览器 F12 控制台，敲：

```js
// 1. 看 api.js 有没有加载
typeof API

// 2. 注册一个测试账号
await API.register('测试账号001', 'abc123456')

// 3. 登录
await API.login('测试账号001', 'abc123456')

// 4. 看 token 存下来没
localStorage.getItem('token')

// 5. 用 token 拿自己的信息
await API.getMe()

// 6. 退出
API.logout()
```

一条条敲，看每步返回什么。**这比看文档理解快得多。**
