# 第 10 阶段完成说明 —— 部署上线

> 这一阶段做的事情：**让项目真正上线，别人能通过公网地址访问。**
>
> 最终方案：**Cloudflare 快速隧道**（免账号、免服务器、免费用）。
> 代码同时已推送到公开仓库 https://github.com/uhe23Md/chat-app

---

## 一、两条路的取舍

原本计划走 **GitHub + Render**，实际执行时改成 **Cloudflare 隧道**。原因：

| | GitHub + Render | Cloudflare 隧道（最终选的） |
|---|---|---|
| 需要账号 | GitHub + Render 两个 | **不需要** |
| 需要配 SSH 公钥 | 是（卡住了） | 否 |
| 每次改代码要重新部署 | 是（等几分钟） | 否（本机实时生效） |
| 地址是否固定 | 固定 | **每次重启变** |
| 电脑关机后还能访问 | 能 | **不能** |
| 数据库会不会丢 | 免费层重启就丢 | 不丢（在本机磁盘） |

选隧道是因为：**你要的是"能发给别人看"，不是"7×24 运行"**。
隧道在这一点上完胜——省掉两个账号和一堆配置，改完代码立刻生效。

代码仓库那条路也没浪费：代码已经推到 GitHub，将来想真正 7×24 部署，
随时可以在 Render 上点几下就起来（`render.yaml` 都备好了）。

---

## 二、怎么用（三步）

项目根目录里有三个双击就能用的文件：

### 启动公网访问.cmd ← 双击这个就行

它会依次：
1. 启动聊天室服务（本机 3000 端口）
2. 开一条 Cloudflare 隧道，拿到一个公网地址
3. **自动验证这个地址能不能真的聊天**（包括 WebSocket 实时收发）
4. 把地址存到桌面「聊天室公网地址.txt」

跑完屏幕上会显示类似：

```
  这个链接可以发给别人了（手机、其他电脑都能打开）：

  https://xxxx-xxxx-xxxx.trycloudflare.com
```

**把这个链接发给别人，手机、平板、其他电脑都能打开。**

### 停止公网访问.cmd

关掉服务和隧道，公网地址立即失效。

### 隧道控制台.cmd

前台运行隧道，实时刷日志。想确认"现在有没有人在访问"时用。

---

## 三、必须知道的限制

**1. 电脑关机 / 关掉那个黑窗口 = 链接立即失效。**

这是隧道的本质：流量是从 Cloudflare 转发到**你这台电脑**的。
本机一停，链接自然就打不开了。页面会显示 Cloudflare 的 530 错误。

**2. 每次重新启动，地址会变。**

免账号隧道没有固定域名。想要固定地址，需要：
- 注册 Cloudflare 账号，建一条命名隧道（免费），可以绑定自己的域名
- 或者改用 Render 那种传统部署

**3. 免账号隧道没有可用性保证。**

Cloudflare 官方说明里写了：这类隧道"没有 uptime guarantee"，
适合演示和测试，不适合放正式业务。

**4. 数据存在本机。**

和 Render 免费层相反 —— 这里的数据库就在项目目录下（`chat.db`），
**重启不丢数据**，反而是优点。但也意味着：别人访问到的数据，
其实是存在你电脑上的。

---

## 四、这一阶段写的代码

### 4.1 部署加固（`server/config.js`）

把部署相关的配置全部收进一个文件：

```js
const IS_CLOUD =
  process.env.NODE_ENV === 'production' ||
  !!process.env.RENDER || !!process.env.RAILWAY_ENVIRONMENT ||
  !!process.env.DYNO || !!process.env.FLY_APP_NAME ||
  !!process.env.WEBSITE_SITE_NAME;

const PORT = Number(process.env.PORT) || 3000;
const DB_PATH = path.resolve(__dirname, '..', process.env.DB_PATH || './chat.db');
```

**三个关键点，每个都有踩坑背景：**

**端口必须读 `process.env.PORT`。** 云平台会自己注入这个变量并据此做健康检查。
写死 3000 会导致平台报 `service did not become reachable on port 10000 within 60s`。

**数据库路径必须 `path.resolve` 成绝对路径。** 相对路径是按**进程的工作目录**解析的，
不是按代码文件位置。本地 `npm start` 时工作目录正好是项目根，看不出问题；
线上从别的目录拉起进程，就会在奇怪的地方新建一个空库 —— 现象是"数据全丢了"。

**CORS 白名单必须放行没有 `Origin` 头的请求：**

```js
if (!origin) return callback(null, true);
```

同源请求、curl、平台健康检查**都不带 `Origin` 头**。卡这一条会导致
"自己的前端调不通自己的后端"，而且现象很迷惑（浏览器报 CORS 错，curl 测又是好的）。

拒绝时用 `callback(null, false)` 而不是 `callback(new Error(...))`：
用 Error 会让请求变成 500，日志里一堆假错误；用 false 只是不发 CORS 放行头，
浏览器正常拦截，干净。

### 4.2 监听 `0.0.0.0`

```js
server.listen(PORT, '0.0.0.0', () => { ... });
```

默认只绑 `127.0.0.1`，容器环境下容器外的健康检查连不上。
这个坑很隐蔽：日志显示"服务已启动"，但平台就是判定不健康。

### 4.3 启动日志打印环境信息

```
========================================
  聊天室后端已启动 [线上模式]
  端口: 10000
  CORS: 已配置白名单 1 条
  健康检查: /api/health
========================================
```

部署时一眼就能看出配置对不对：显示 `[本地模式]` 说明 `NODE_ENV` 没设上。

### 4.4 `.gitignore` 补了 WAL 附属文件

原来的规则是 `*.db` + `*.db-journal`，**漏了 `*.db-shm` 和 `*.db-wal`**。
SQLite 开 WAL 模式后会常驻这两个文件，里面存着还没合并进主库的数据。
实测抓到了 —— `chat.db-wal` 有 2.2MB，差点进了仓库。

### 4.5 `.gitattributes` 锁定行尾

```gitattributes
*.cmd  text eol=crlf
*.bat  text eol=crlf
*.js   text eol=lf
```

**为什么必须显式锁定？** 批处理文件用 LF 换行的话，cmd.exe 解析
`goto` / `if` / `for` 这些控制流会出错，典型症状是"脚本跑到一半莫名结束"
或"标签找不到"。不能依赖 git 的 `core.autocrlf` 自动转换 ——
不同机器 clone 下来可能不一样。

---

## 五、踩坑记录（都是实测出来的）

写这几个 `.cmd` 脚本踩了一堆坑，每一条都是**跑起来才发现的**，
值得记下来：

### 坑 1：`.cmd` 里的中文注释会把换行吃掉

```
'��时隧道，拿到一个公网地址' is not recognized as an internal or external command
```

**根因**：cmd.exe 用**系统代码页**（本机是 GBK）读取整个 .bat 文件。
UTF-8 的中文字节被按 GBK 双字节解析，**中文的第二个字节会把紧跟的
ASCII 字符（引号/括号/换行）吃掉**，导致注释行被截断，后半截变成了命令。

**解法**：**`.cmd` 里除了 `echo` 输出的中文，其他一律用 ASCII**
（注释、标签、变量名）。`echo` 的中文没问题，因为 `chcp 65001` 生效了。

> 这和 `.ps1` 遇到的是同一个坑（见用户级记忆）。Windows 上的脚本编码问题
> 本质都是一样的：**解释器用什么编码读文件，决定了你能不能写非 ASCII**。

### 坑 2：给 `.cmd` 加 UTF-8 BOM 反而更糟

加了 BOM 之后：

```
'@echo' is not recognized as an internal or external command
C:\...>echo ============================================================
C:\...>echo   正在启动聊天室 + 公网隧道
```

`@echo off` 整个失效，屏幕被回显刷满。

**根因**：cmd 把 BOM 的三个字节当成了第一条命令的一部分。
**结论：`.cmd` 千万不要加 BOM。**

### 坑 3：`timeout /t` 在装了 Git Bash 的机器上会坏

```
timeout: invalid time interval '/t'
```

**根因**：机器上装了 Git Bash，PATH 里有一个 Unix 的 `timeout` 命令，
把 Windows 的 `timeout.exe` 抢走了。结果脚本里的等待循环全部失效。

**解法**：用 `ping -n 2 127.0.0.1 >nul` 做延时（Windows 全版本通吃，
不依赖 PATH）。想要更短的延时用 `ping -n 1 -w 200`。

### 坑 4：子过程定义的位置会影响主流程

批处理是**从上往下顺序执行**的。如果 `call :sub` 之后紧跟着 `:sub` 的定义，
主流程执行完会**掉进子过程**，然后 `goto :eof` 直接结束脚本 ——
后面所有代码永远不执行。

**解法**：**所有子过程定义放到文件最末尾**，主流程末尾加 `exit /b 0` 明确退出。

### 坑 5：`for %%t in ("字符串")` 不会按空格拆开

我以为 `for %%t in ("a b c")` 能拆出 a、b、c 三个 token。
**实际不能** —— 引号里的整串被当成**一个** token。

原本想用"先把竖线替换成空格，再分词"的方式解析 cloudflared 的日志：

```
2026-09-16T14:20:03Z INF |  https://xxx.trycloudflare.com  |
```

结果拿到的是**整行**，判断 `%S:~0,8%` 是否等于 `https://` 永远为假。

**解法**：改用**纯字符串替换**逐步剥掉前后缀：

```bat
set "TMP=!RAWLINE:*https://=https://!"   REM 删掉 https:// 之前的所有内容
set "TMP=!TMP: =!"                        REM 删掉所有空格
set "TMP=!TMP:|=!"                        REM 删掉竖线
```

实测在 4 种日志格式下都正确（含真实框线格式、无框线格式、行首格式、
以及不该匹配的 metrics 行）。

### 坑 6：`findstr` 传两个文件会加文件名前缀

```bat
findstr /R /C:"..." "a.log" "b.log"    REM 输出是 "a.log:匹配内容"
```

拿到带前缀的字符串，解析必然失败。

**解法**：只搜一个文件。另外 cloudflared 在 Windows 上把日志全部写到
**stderr**，stdout 是空的，所以搜 `.err` 那个文件。

### 坑 7：隧道刚建好时 DNS 还没生效

```
getaddrinfo ENOTFOUND xxx.trycloudflare.com
```

隧道进程一启动，Cloudflare 就开始分配域名，但**域名要在边缘节点上线
还需要几秒**。这期间请求会拿到 `ENOTFOUND`（不是 4xx/5xx，是根本没连上）。

这个错误看起来和"隧道坏了"一模一样，很容易误判。

**解法**：验证脚本里加轮询等待（`waitReady()`，最多 60 秒），
脚本里也用 `nslookup` 判断 DNS 是否就绪。**把"还没准备好"和"真的坏了"区分开。**

### 坑 8：`wait_tunnel` 匹配太松，被启动横幅骗了

cloudflared 启动时会打印：

```
INF Requesting new quick Tunnel on trycloudflare.com...
```

我原本用 `findstr /C:"trycloudflare.com"` 判断"地址出现了"，
结果**这行横幅就命中了**，循环提前退出，但此时地址还没生成，
解析出来是空的。

**解法**：匹配完整的 URL 正则 `https://[a-z0-9-]*\.trycloudflare\.com`，
而不是裸域名。

### 坑 9：`.cmd` 文件的编码必须是「无 BOM + CRLF」

总结成一句话：**`UTF-8 无 BOM` + `CRLF 换行` + `注释全 ASCII` + `中文只放 echo`**。

这四条同时满足，脚本才能在中文 Windows 上稳定运行。

---

## 六、验证

### 6.1 自动化测试

| 测试 | 项数 | 结果 |
|---|---|---|
| 后端：注册登录鉴权 | 18 | ✅ |
| 后端：Socket 实时层 | 53 | ✅ |
| 浏览器：登录注册页 | 52 | ✅ |
| 浏览器：聊天功能 | 67 | ✅ |
| 部署：线上模式端到端 | 38 | ✅ |
| 启动脚本逻辑 | 18 | ✅ |
| cmd 语法边界 | 4 | ✅ |
| 公网隧道端到端 | 15 | ✅ |
| **合计** | **265** | **全过** |

新增 npm 命令：

```bash
npm run test:deploy     # 部署验证（自动起服务）
npm run test:tunnel     # 公网隧道验证（需先启动隧道）
npm run test:launcher   # 启动脚本逻辑测试
npm run security        # 公开仓库前安全体检
npm run test:all        # 后端 + 部署 + 启动脚本
```

### 6.2 公网隧道实测（15 项）

```
【1. 基础连通性】健康检查 200 / 后端响应正常 / 登录页 / 聊天页
【2. 注册与登录】注册 / 登录拿 token / 第二用户登录
【3. WebSocket 实时连接（关键）】
   两端连接成功 / 获取房间列表 / 都加入房间 / 发消息收到 ack
   ★ 对方实时收到消息（WebSocket 双向通）
   ★ 私聊实时到达
   消息已存入数据库
```

**第 3 节是关键。** WebSocket 在生产环境走的是另一种传输路径，
隧道对 upgrade 的支持、CORS 配置对不对，只有这里才暴露。

顺带一个发现：本机 UDP 7844（cloudflared 默认的 QUIC 传输）被墙时，
它会自动降级到 `http2` 传输 —— 而这个模式下 **WebSocket 依然正常工作**
（Cloudflare 边缘处理了 upgrade）。

### 6.3 公开仓库安全体检

`npm run security` 会逐项检查：

```
1. 密钥文件         .env / .deploy-secret.txt 是否被忽略
2. 数据库文件        chat.db / -shm / -wal 是否被忽略
3. 已跟踪文件        有没有密钥、数据库、node_modules
4. 文件内容          有没有硬编码的密钥、私钥块、云厂商 key
5. git 历史          ★ 历史里有没有泄露过（有过就永远算泄露）
6. 远端仓库地址       提醒确认是公开还是私有
```

**第 5 项是最关键的。** 一旦密钥进了公开仓库，即使你马上删掉，
GitHub 的抓取机器人也早就存下来了 —— **这类泄露不可逆**。

实际跑的结果：

```
✅ 安全体检通过
   远端 64 个文件
   ✓ 没有 .env / 数据库 / node_modules
   ✓ git 历史里从未出现过密钥 / 数据库文件
```

> 顺带修了扫描器自己的一个误报：`.env.example` 是**模板文件**，
> 里面全是占位值（`change_me_to_a_random_long_string`），
> 它**本来就该**进公开仓库 —— 别人 clone 下来照着填自己的值。
> 一刀切地把 `.env*` 全判为危险会造成误报，反而让人忽略真正的告警。

---

## 七、git 状态

```
715e7f9  公网访问：一键启动隧道 + 安全体检 + 行尾锁定
ded0def  部署工具：新增进度检查脚本（deploy/status.cjs）
0e73f01  部署工具：一键推送脚本 + Render 配置清单
c34fbb6  文档：README 更新项目实际状态，新增第 10 阶段完成说明
856d5b6  部署加固：环境配置集中化 + Render 蓝图 + 部署验证脚本
a232f75  聊天室 v1.0：登录鉴权 + 多房间实时聊天 + 私聊
```

**已推送到** https://github.com/uhe23Md/chat-app（公开仓库，64 个文件）

推送方式：**HTTPS + Git Credential Manager**。
之前折腾 SSH 公钥一直失败（`Permission denied (publickey)`），
后来发现直接用 HTTPS 就行 —— 凭据管理器已经存好了认证信息，
**公开仓库根本不需要配 SSH 公钥**。

> 教训：遇到认证问题先试最简单的路（HTTPS），
> 别一上来就折腾 SSH 密钥这种"看起来更专业"的方案。

---

## 八、下一步可以做什么

**如果只是给人演示** —— 现在这样就够了，双击脚本就行。

**如果想 7×24 挂着** —— 三条路：

1. **Cloudflare 命名隧道**（免费，需注册账号）
   建一条命名隧道可以绑定自己的域名，地址固定。
   本机还是得开着，但地址不会变。

2. **Render 免费层**（需 GitHub 账号，代码已经推上去了）
   照 `部署指南.md` 走，`render.yaml` 已备好。
   注意：免费层容器文件系统是临时的，**每次重新部署数据会重置**。

3. **升级付费 + 挂持久卷**（约 $7/月）
   改一个环境变量 `DB_PATH=/data/chat.db` 就行。

**如果要继续开发** —— 第 9 阶段列的这些还没做：
消息已读、正在输入、未读红点、消息分页加载更多。

---

## 九、关于"数据丢失"

三种方案下数据的归宿完全不同：

| 方案 | 数据存哪 | 重启后 |
|---|---|---|
| **当前（隧道）** | 本机 `chat.db` | **不丢** |
| Render 免费层 | 容器临时文件系统 | **全丢** |
| Render + 持久卷 | 挂载的卷 | 不丢 |

好消息是：不管换哪种，**改动都集中在数据访问层**。
`queries.js` 是唯一写 SQL 的地方（第 6 阶段定下的架构决策），
换 Postgres 也只需要改这一个文件，业务逻辑一行都不用动。
