# 第 10 阶段完成说明 —— 部署上线

> 这一阶段做的事情：**让项目具备部署能力，并验证部署后真的能用。**
>
> 注意区分：「代码改好了」和「已经部署到线上」是两回事。本文档完成的是前者 + 完整的部署前验证；
> 真正点下 Render 的部署按钮需要你有 GitHub 账号（见 `部署指南.md`，跟着做 15 分钟搞定）。

---

## 一、这一阶段解决了什么问题

项目在前 9 个阶段都只能在本机 `localhost:3000` 跑，别人看不到、手机打不开。但"能部署"不只是"传上去"那么简单——**本地跑得通和线上跑得通，中间隔着一堆环境差异**：

| 本地 | 线上 | 不处理的后果 |
|---|---|---|
| 端口固定 3000 | 平台注入 `PORT` | 健康检查失败，服务反复重启 |
| CORS 全开（`origin: '*'`） | 必须收紧 | 任何人能跨站调你的接口 |
| 不区分环境 | `NODE_ENV=production` | 线上跑着开发配置 |
| 数据库放项目根目录 | 可能要放持久卷 | 部署后数据丢失 |

这一阶段把这些差异全部收敛到**一个文件**里管理。

---

## 二、核心改动：`server/config.js`

新增了这个文件（约 190 行），是本次改动的核心。

**为什么要把配置抽出来？** 因为部署时你要改的东西散落在 `index.js`、`db.js`、`sockets/index.js`、`routes/messages.routes.js` 四个地方，改漏一个就出问题。集中之后，**部署前只需要看这一个文件**。

### 2.1 云端环境自动检测

```js
const IS_CLOUD =
  process.env.NODE_ENV === 'production' ||
  !!process.env.RENDER ||
  !!process.env.RAILWAY_ENVIRONMENT ||
  !!process.env.DYNO ||
  !!process.env.FLY_APP_NAME ||
  !!process.env.WEBSITE_SITE_NAME;
```

**为什么要自动检测、而不是让你手动设？** 因为漏设 `NODE_ENV` 是最常见的部署事故。结果是线上跑着开发配置（CORS 全开、错误信息全暴露）。多一层保险。

覆盖了 5 个平台：Render / Railway / Heroku / Fly.io / Azure。换平台不用改代码。

### 2.2 端口必须读环境变量

```js
const PORT = Number(process.env.PORT) || 3000;
```

**这是踩过的坑。** 之前本地开发时端口写死 3000，部署上去平台报：

```
service did not become reachable on port 10000 within 60s
```

原因：云平台会**自己注入 `PORT`**，然后用它做健康检查。你的服务不读它、还监听 3000，平台就找不到服务，判定启动失败然后反复重启。

⚠️ 这条和 `.env` 里的 `PORT=3000` 不冲突：线上**不要设** `PORT` 环境变量，让它由平台注入。

### 2.3 CORS 三分支策略

```js
function corsOptions() {
  // 本地 → origin: true（放开，方便用 Live Server 调试）
  // 线上 + 有白名单 → 严格校验 ALLOWED_ORIGINS
  // 线上 + 无白名单 → 放行 + 打印警告
}
```

**一个容易踩的细节**：白名单校验里必须**放行没有 `Origin` 头的请求**：

```js
if (!origin) return callback(null, true);
```

因为同源请求、curl、平台健康检查**都不带 `Origin` 头**。卡这一条会导致"自己的前端调不通自己的后端"——而且现象很迷惑（浏览器直接报 CORS 错，但用 curl 测又是好的）。

另一个细节：拒绝时用 `callback(null, false)` 而不是 `callback(new Error(...))`。用 Error 会让请求变成 500，日志里一堆假错误；用 `false` 只是不发 CORS 放行头，浏览器会正常拦截，干净利落。

### 2.4 数据库路径要 resolve 成绝对路径

```js
const DB_PATH = path.resolve(__dirname, '..', process.env.DB_PATH || './chat.db');
```

**为什么不能留着 `'./chat.db'` 这个相对路径？** 相对路径是按**进程的工作目录**解析的，不是按代码文件位置。本地 `npm start` 时工作目录正好是项目根目录，看不出问题；但线上平台常常从别的目录拉起进程，相对路径就会在奇怪的地方新建一个**空数据库**——现象是"数据全丢了"，实际是连到了另一个文件。

---

## 三、其他改动

### 3.1 监听 `0.0.0.0`（`server/index.js`）

```js
server.listen(PORT, '0.0.0.0', () => { ... });
```

默认只绑 `127.0.0.1`，容器环境下容器外的健康检查连不上。这个坑很隐蔽：日志显示"服务已启动"，但平台就是判定不健康。

### 3.2 启动日志打印环境信息

现在启动时会明确显示：

```
========================================
  聊天室后端已启动 [线上模式]
  端口: 10000
  CORS: 已配置白名单 1 条
  健康检查: /api/health
========================================
```

**部署时这一眼就能看出配置对不对**：显示 `[本地模式]` 说明 `NODE_ENV` 没设上；端口显示 3000 说明没读到平台注入的 `PORT`。

### 3.3 修了一个 `.gitignore` 漏洞

原来的规则是：

```
*.db
*.db-journal
```

**漏了 `*.db-shm` 和 `*.db-wal`。** SQLite 开了 WAL 模式后会常驻这两个附属文件（共享内存索引 + 预写日志），它们和 `.db` 一样是运行时产物，里面存着还没合并进主库的数据。

实际检查时确实抓到了——`chat.db-wal` 有 1.5MB，差点进了仓库。已补上规则。

### 3.4 `render.yaml` 部署蓝图

把 Render 的配置写进版本库，重建服务时不用重新回忆各字段。文件里包含了**免费套餐的限制说明**和**付费持久卷方案的注释模板**。

### 3.5 `部署指南.md`

完整的上线文档，包括：
- 部署前必须改的三件事（含"为什么"）
- 平台对比（为什么选 Render，为什么 Vercel 不行）
- 逐步操作 + 验证方法
- **故障排查**（6 个常见问题，含症状→原因→解法）
- 免费套餐的天花板

---

## 四、验证：新增部署验证脚本

这是本阶段**最有价值的部分**。

### 4.1 为什么要专门写这个脚本

本地默认跑法（`npm start`）测不到线上环境的问题：

- 端口是 3000 而不是平台注入的端口
- CORS 是全开的，白名单逻辑根本没执行
- `NODE_ENV` 没设为 production

**所以这个脚本用"线上参数"启动服务**，然后跑一遍完整业务链路：

```js
spawn('node', ['server/index.js'], {
  env: {
    NODE_ENV: 'production',
    PORT: '10000',                                    // 模拟平台注入
    JWT_SECRET: 'verify-deploy-secret-...',
    ALLOWED_ORIGINS: 'https://chat-app-demo.onrender.com',
    DB_PATH: '.verify-deploy-test.db',                // 独立测试库，不污染开发库
  },
});
```

### 4.2 覆盖的 7 个环节（38 项断言）

| 环节 | 验证内容 |
|---|---|
| **1. 云端环境识别** | 日志显示 `[线上模式]`、端口读到 10000、CORS 白名单被识别 |
| **2. 健康检查与静态资源** | `/api/health` 200、登录页可访问、Socket.IO 客户端脚本可访问 |
| **3. CORS 白名单行为** | 白名单内放行、白名单外不发放行头、无 Origin 头正常、预检 204 |
| **4. 注册与登录** | 注册成功、token 是 JWT 三段式、`/api/me` 正确、无 token 401 |
| **5. 房间与历史消息** | 3 个默认房间、能拉历史、不存在的房间 404 |
| **6. Socket.IO 实时连接** | ★ WebSocket 连接、在线列表去重、实时收发、持久化、私聊、伪造 token 被拒 |
| **7. 数据持久化路径** | 数据库建在 `DB_PATH` 指定位置 |

**第 6 节是关键。** WebSocket 在生产环境下走的是另一种传输路径，很多部署问题（反代不支持 upgrade、CORS 配错）只有在这里才暴露。

### 4.3 结果

```
通过 38 项，失败 0 项
```

### 4.4 顺带修了脚本自己的两个 bug

写测试脚本时踩到的坑，记录一下（都是**测试逻辑**问题，不是服务问题）：

**坑一：两个接口的响应结构不一样**

```
POST /api/register → { ok, data: { id, username, avatar, createdAt } }        ← 扁平
POST /api/login    → { ok, data: { token, user: { id, username, avatar } } }  ← 嵌套
```

我一开始两处都按 `data.user.id` 取，结果 `register` 那处拿到 `undefined`。**同一个项目里不同接口的响应结构不一致，是很容易犯的错**——写测试时踩到，也算提前暴露了 API 设计上的一致性债。

**坑二：`online_users` 只在成员真的变化时才广播**

我原本想用"让已在房间的 alice 重复 `join_room`"来触发在线列表广播，结果永远等不到。原因是服务端对重复加入做了幂等处理（这是对的，避免重复广播）。

正确做法是引入**第三个用户** carol 加入，触发真正的成员变化。改完之后不仅测到了在线列表，还顺带验证了**去重逻辑**（同一用户多连接只出现一次）。

---

## 五、测试总览

| 测试 | 项数 | 结果 |
|---|---|---|
| 后端：注册登录鉴权 | 18 | ✅ |
| 后端：Socket 实时层 | 53 | ✅ |
| 浏览器：登录注册页 | 52 | ✅ |
| 浏览器：聊天功能（4 个浏览器实例） | 67 | ✅ |
| **部署：线上模式端到端** | **38** | ✅ |
| **合计** | **228** | **全过** |

新增的 npm 命令：

```bash
npm run test:deploy    # 部署验证（自动起服务，约 20 秒）
npm run test:browser   # 两个浏览器测试
npm run test:all       # 后端 + 部署
```

---

## 六、git 状态

```
856d5b6  部署加固：环境配置集中化 + Render 蓝图 + 部署验证脚本
a232f75  聊天室 v1.0：登录鉴权 + 多房间实时聊天 + 私聊
```

仓库里 50 个文件。**确认过 `.env`、`chat.db*`、`node_modules` 都没有入库**（用 `git status --short` 逐条核对过）。

---

## 七、下一步：真正部署

代码这边准备好了。真正上线还需要你做一件事：**推 GitHub**。

```bash
cd C:\Users\Administrator\Desktop\1\chat-app
git remote add origin https://github.com/你的用户名/chat-app.git
git branch -M main
git push -u origin main
```

然后照 `部署指南.md` 第 2 步在 Render 上建服务。

**部署完记得回来告诉我**，我帮你：
1. 检查线上日志有没有异常
2. 验证线上实时聊天是否正常
3. 如果数据丢失问题不能接受，改造成持久化方案

---

## 八、关于"数据丢失"这件事

必须提前说清楚：**Render 免费套餐的容器文件系统是临时的，每次重新部署，用户和消息都会重置。**

这不是 bug，是免费套餐的设计。对"演示给别人看"够用，但别把重要数据放上去。

三条路（细节见 `部署指南.md` 第五节）：

1. **接受**（演示场景够用）
2. **升级付费 + 挂持久卷**（约 $7/月，改一个环境变量 `DB_PATH=/data/chat.db`）
3. **换 Postgres**（长期方案，要改 `db.js` 和 `queries.js` 的 SQL 方言）

好消息是：不管选哪条，**改动都集中在数据访问层**（`queries.js` 是唯一写 SQL 的地方，这是第 6 阶段就定下的架构决策）。业务逻辑一行都不用动。
