# 部署指南 —— 把聊天室上线到公网

> 目标：拿到一个 `https://xxx.onrender.com` 的链接，发给任何人（包括手机）都能打开、注册、聊天。
>
> 全程不需要信用卡，不需要服务器，不需要域名。

---

## 目录

1. [部署前必须改的三件事](#一部署前必须改的三件事)
2. [为什么选 Render](#二为什么选-render)
3. [完整步骤](#三完整步骤)
4. [常见问题排查](#四常见问题排查)
5. [免费套餐的天花板](#五免费套餐的天花板)
6. [如果想换平台](#六如果想换平台)

---

## 一、部署前必须改的三件事

这三件事**不做，部署上去就是坏的**。项目代码已经准备好了，你只需要改配置。

### 1. `JWT_SECRET` 必须是随机值

本机 `.env` 里现在是一个值，但这个值**绝不能带到线上**——它已经在你的本地文件里躺了很久，而且 `.env` 在 `.gitignore` 里、不会进仓库，所以线上服务读不到它，需要用平台的环境变量面板单独设一个。

怎么生成一个新的：

```bash
node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
```

会输出一长串十六进制字符，复制下来，后面填到 Render 的环境变量里。

> 为什么这件事重要：`JWT_SECRET` 是用来签发登录凭证的密钥。谁拿到它，谁就能伪造任意用户的登录态——不用密码就能登录你的账号。所以本地一个、线上一份，两边不共用。

### 2. 端口不能写死（这个项目已经改好了）

之前踩过这个坑：本地开发时端口写死 3000，部署上去平台报

```
service did not become reachable on port 10000 within 60s
```

原因：云平台会**自己注入一个 `PORT` 环境变量**，然后用这个端口来做健康检查。你的服务如果不读它、还是监听 3000，平台就找不到你的服务，判定启动失败。

**本项目已经处理好了**——`server/config.js` 里：

```js
const PORT = Number(process.env.PORT) || 3000;
```

有 `PORT` 就用平台的，没有（本地开发）才退回 3000。你不需要改任何代码。

### 3. CORS 要从 `*` 收紧（这个项目也已经改好了）

原来是 `cors: { origin: '*' }`，等于告诉浏览器"任何网站都可以调我的接口"。虽然因为有 token 保护、实际拿不到数据，但这是坏习惯。

**本项目也已经处理好了**——`server/config.js` 里会自动判断：

| 场景 | 行为 |
|---|---|
| 本地开发 | 放开所有来源（方便你调试） |
| 线上 + 配了 `ALLOWED_ORIGINS` | 严格白名单，只允许列出的域名 |
| 线上 + 没配白名单 | 放行，但启动时打印警告 |

部署时建议在环境变量里加上 `ALLOWED_ORIGINS`，填上部署后的真实域名。

---

## 二、为什么选 Render

对比了三个免费平台：

| 平台 | 免费额度 | Node 版本 | WebSocket | 国内访问 | 结论 |
|---|---|---|---|---|---|
| **Render** | 750 小时/月 | 可选 22 ✅ | 支持 ✅ | 一般，可访问 | **选它** |
| Railway | 试用 $5 额度，之后付费 | 可选 | 支持 | 可以 | 额度用完就停 |
| Vercel | 免费 | 可选 | **不支持长连接** ⚠️ | 需要特殊处理 | **不适合本项目** |
| Fly.io | 免费额度少 | 可选 | 支持 | 可以 | 需绑卡 |

**Vercel 为什么不适合**：它是 Serverless 架构，每个请求起一个临时函数实例，请求完就销毁。而聊天室依赖 WebSocket 长连接——连接必须一直挂着。Serverless 天然做不到这件事。

**Render 的注意点**：免费实例**15 分钟没有流量就会休眠**。休眠后再有人访问，冷启动要 30~60 秒。第一次打开慢是正常的，不是坏了。

> **好消息（2026-02-24 起）**：Render 调整了免费实例的休眠策略——**正在接收 WebSocket 消息的服务不会休眠**。这意味着聊天室只要有人开着页面在聊，就不会进入冷启动。只有全员离线 15 分钟后才会睡，此时下一个人打开需要等 30~60 秒。对演示场景来说完全够用。

---

## 三、完整步骤

### 第 0 步：确认本地测试全过

```bash
npm test                # 后端 71 项
cd .verify
node verify-stage5.cjs   # 前端 52 项
node verify-stage6.cjs   # 聊天 67 项
```

**本地全绿再部署。** 线上排查问题比本地麻烦十倍——本地能复现的问题不要带到线上。

### 第 1 步：推到 GitHub

先确认敏感文件没被提交：

```bash
git init
git status
```

在 `git status` 的输出里，**必须看不到** `.env` 和 `chat.db`（`.gitignore` 已经排除了它们）。如果看到了，说明 `.gitignore` 没生效，停下来检查。

确认无误后：

```bash
git add .
git commit -m "聊天室：完成登录鉴权、多房间实时聊天、私聊"
```

然后去 GitHub 网页上新建一个**空的**仓库（不要勾选 Add README / .gitignore，避免和本地冲突），拿到仓库地址后：

```bash
git remote add origin https://github.com/你的用户名/chat-app.git
git branch -M main
git push -u origin main
```

> 如果 `git push` 提示要登录：GitHub 从 2021 年起不能再用密码推送。用 Personal Access Token 代替密码——GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token，勾上 `repo` 权限，生成的字符串当密码用。

### 第 2 步：在 Render 创建服务

1. 打开 <https://render.com>，用 GitHub 账号登录（**不需要信用卡**）
2. 点右上角 **New +** → **Web Service**
3. 选 **Build and deploy from a Git repository** → **Next**
4. 找到刚才推上去的 `chat-app` 仓库，点 **Connect**
5. 填配置：

| 字段 | 填什么 |
|---|---|
| **Name** | `chat-app`（这会变成域名的一部分） |
| **Region** | `Singapore`（离国内最近） |
| **Branch** | `main` |
| **Runtime** | `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `npm start` |
| **Instance Type** | `Free` |

6. 往下拉，点 **Advanced** → 加环境变量（点 **Add Environment Variable**）：

| Key | Value |
|---|---|
| `NODE_ENV` | `production` |
| `NODE_VERSION` | `24.14.1` |
| `JWT_SECRET` | 第 1 步生成的那串随机字符 |

> `NODE_VERSION` 必须设！本项目用了 Node 内置的 `node:sqlite`，需要 **Node 22.5 以上**。Render 目前的默认版本是 24.14.1（2026-04-21 起），理论上不设也行，但**默认版本会随时间变化**，显式锁住更稳。这里选 24.14.1 是为了**和本地开发环境保持一致**（本机是 v24.21.0），避免版本差异带来的隐蔽问题。

7. 点 **Create Web Service**

### 第 3 步：等构建完成

Render 会开始拉代码、装依赖、启动服务。第一次大约 2~4 分钟。日志里你应该看到：

```
[db] 数据库就绪: /opt/render/project/src/chat.db
[db] 当前房间数: 3
[socket] Socket.IO 实时层已就绪

========================================
  聊天室后端已启动 [线上模式]
  端口: 10000
  CORS: ⚠️  未配置白名单，当前放行所有来源
  健康检查: /api/health
========================================
```

**看到 `[线上模式]` 就说明环境检测生效了**（`NODE_ENV=production` 被识别到）。端口是 `10000` 而不是 3000，说明读到了平台注入的 `PORT`——这两点都对，就成功了。

如果显示 `[本地模式]`，说明 `NODE_ENV` 没设上，回第 2 步检查。

### 第 4 步：验证线上服务

Render 面板左上角会显示你的域名，形如 `https://chat-app-xxxx.onrender.com`。

**先测后端**——浏览器打开（把域名换成你的）：

```
https://chat-app-xxxx.onrender.com/api/health
```

应该返回：

```json
{"ok":true,"data":{"message":"聊天室后端运行中",...}}
```

**再测前端**——打开根地址：

```
https://chat-app-xxxx.onrender.com
```

应该自动跳到登录页。注册一个账号，开两个浏览器窗口（或一个正常窗口 + 一个无痕窗口）用两个账号登录，互发消息验证实时性。

### 第 5 步：补上 CORS 白名单（可选但推荐）

回到 Render 面板 → 你的服务 → **Environment** → 加一条：

| Key | Value |
|---|---|
| `ALLOWED_ORIGINS` | `https://chat-app-xxxx.onrender.com`（换成你的真实域名，**不要带结尾斜杠**） |

保存后 Render 会自动重新部署。再看日志，`CORS:` 那行会变成 `已配置白名单 1 条`。

---

## 四、常见问题排查

### 启动失败：`Cannot find module 'node:sqlite'`

Node 版本太低（需要 22.5+）。检查 Render 的 `NODE_VERSION` 环境变量是否设成了 `24.14.1` 或更高。

### 构建成功但健康检查失败

日志里找这行：`聊天室后端已启动 [线上模式]` 后面跟的端口号。

- 如果是 `3000` → `NODE_VERSION`/`PORT` 处理有问题，但本项目代码已经处理了，大概率是环境变量设错了
- 如果是 `10000` → 端口没问题，那可能是启动时抛异常了，往上翻日志找 `[error]`

### 页面能打开但登录报错 / 转圈

打开浏览器 F12 → **Network** 面板，看 `/api/login` 这个请求：

- **状态码 500** → 服务端报错，去 Render 日志里找 `[error]`
- **状态码 0 / CORS error** → 你在**本地**打开的是线上的前端页面？不是的话，检查 `ALLOWED_ORIGINS` 是否写成了错误的域名

### 消息发出去对方收不到

F12 → **Console** 面板，找 socket.io 的报错。常见原因：

- 连接建立失败 → 检查 Network 里 `polling` 或 `websocket` 请求是否 400/401
- 401 → token 过期或 `JWT_SECRET` 被改了（重新生成 `JWT_SECRET` 会让所有旧 token 失效，所有人需要重新登录）

### 重新部署后数据全没了

**这是预期行为，不是 bug。** 免费套餐的容器文件系统是临时的，每次部署都会重置。见下一节。

### 第一次打开特别慢（30~60 秒）

免费实例 15 分钟无流量会自动休眠。有人访问时冷启动。这不影响功能，受不了就升级付费套餐（$7/月起）。

---

## 五、免费套餐的天花板

上线之前，把这些限制搞清楚，免得后面踩坑：

| 限制 | 影响 | 应对 |
|---|---|---|
| **容器文件系统临时** | 每次重新部署，`chat.db` 重置，所有用户和消息丢失 | 升级付费+挂盘，或换 Postgres，或接受 |
| **15 分钟休眠** | 全员离线后冷启动 30~60 秒 | 一般不用管——有人在线聊天时不会休眠（见下）|
| **750 小时/月** | 一个实例 24 小时跑一个月约 720 小时，刚好够 | 别开多个实例 |
| **带宽 100GB/月** | 纯文字聊天用不完 | 别传图片/文件 |

> 关于休眠：2026-02-24 起，Render 的免费实例**在收发 WebSocket 消息期间保持活跃**。聊天室的核心场景（有人在线聊天）正好命中这条，所以实际体验比想象中好。只有"最后一个人离开 15 分钟后"才会休眠。

### 数据持久化怎么办？

三条路，按投入排序：

**a) 接受丢失（适合演示）**
不动代码。每次重新部署数据库重置。你自己知道就行。

**b) 升级付费实例 + 挂持久卷**
改一个环境变量 `DB_PATH=/data/chat.db`，`render.yaml` 里放开 `disk` 配置（文件末尾有模板）。成本约 $7/月 + 卷费用。

**c) 换成 Postgres（适合长期）**
Render 免费提供 90 天的 Postgres。但要改 `db.js`（换驱动）和 `queries.js`（SQL 方言差异：`AUTOINCREMENT` → `SERIAL`、`?` 占位符 → `$1`）。改动量集中在数据访问层，架构上不难，但要重新跑一遍全部测试。

---

## 六、如果想换平台

本项目是标准 Node.js 应用，**关键改动已经做完了**，换平台基本只需要改环境变量：

- ✅ 读 `PORT` 环境变量 → 所有平台通用
- ✅ 自动检测云环境 → 已覆盖 Render / Railway / Heroku / Fly / Azure
- ✅ 数据库路径可通过 `DB_PATH` 指定
- ✅ CORS 白名单可配置
- ✅ `engines.node >= 22.5.0` 已写入 `package.json`

**Railway**：新建项目 → Deploy from GitHub repo → 加环境变量 → 完事。识别靠 `RAILWAY_ENVIRONMENT`，代码已支持。
**Heroku**：靠 `DYNO` 变量识别，已支持。但 Heroku 免费额度 2022 年就取消了。
**Fly.io**：靠 `FLY_APP_NAME` 识别，已支持。需要装 `flyctl` 并绑卡。

> 唯一天然不兼容的是 Vercel / Netlify 这类 Serverless 平台——它们不支持 WebSocket 长连接。要上这类平台，得把实时层改成轮询或换用第三方推送服务，那就不是"部署"而是"重写"了。

---

## 附：部署检查清单

上线前逐条打勾：

- [ ] 本地 `npm test` 全过（71 项）
- [ ] 本地浏览器测试全过（52 + 67 项）
- [ ] `git status` 里看不到 `.env` 和 `chat.db`
- [ ] GitHub 仓库已建并推送成功
- [ ] Render 环境变量设了 `NODE_ENV=production`、`NODE_VERSION=24.14.1`、`JWT_SECRET=<随机值>`
- [ ] 线上日志显示 `[线上模式]`
- [ ] `/api/health` 返回 `{"ok":true}`
- [ ] 线上能注册账号、能登录
- [ ] 两个浏览器窗口能实时互发消息
- [ ] 刷新页面历史消息还在
- [ ] 手机浏览器打开能正常用（布局不错乱）
- [ ] `ALLOWED_ORIGINS` 已配置（可选）
